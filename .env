# Vite dev mode: axios uses empty baseURL -> requests go through Vite proxy
# For production, create .env.production with VITE_API_BASE_URL set
VITE_API_PROXY_TARGET=https://academia-transport.local
VITE_DEV_PORT=5173
